# Candidate Enumeration

Candidates are enumerated from score records filtered to the requested capability. Cost records are attached only when capability, strategy, executor, dataset, and score identifier match. Missing cost evidence remains visible and can force insufficient-evidence outcomes depending on policy.

## Boundary

AI-IEP-004A.6 is repository-only, synthetic, offline, advisory, and test-only. It does not deploy code, call live providers, write production data, mutate object storage, change cloud configuration, enforce production budgets, activate premium execution, or make procurement recommendations.
