# Route Intelligence Integration

Supervisor Intelligence integrates with Route Intelligence by consuming route safety assessments and schema references from the existing repository-only Route Intelligence foundation.

The integration is read-only and deterministic. It does not invoke hosted providers, choose models, run routing engines, deploy services, or mutate route records.
