# Object Storage Lifecycle

`lifecycle_object_references` provides the database coordination foundation for delivery photos, signatures, receipts, hazard media, exports, temporary uploads, and future storage references.

Purge preview reports object impacts. Legal hold eligibility is stored per object reference. Physical provider-specific deletion is not executed in this phase.
