# Data Lifecycle Foundation

Status: Implemented on `data-lifecycle-foundation`.

This package documents the ODR-019 foundation for lifecycle state, deactivation, deletion requests, retention-policy evaluation, legal holds, purge preview, object-storage coordination, data subject requests, Organization termination, and auditability.

Production data was not modified. Migration `009_data_lifecycle_foundation.sql` must not be applied to production until the approved production rollout gates are met.
