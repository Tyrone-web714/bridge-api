# Data Exit Export

`data_exports` records tenant-scoped export requests with a bounded manifest. Export format and delivery method remain `OWNER_DECISION_REQUIRED`.

The foundation records the workflow and authorization gate; it does not package or deliver customer export archives.
