# Moderation Queue

The queue page lists moderation candidates and supports bounded filtering by:

- review status
- hazard type
- source severity
- source Organization ID
- submitted date range
- sort order
- limit

Source Organization visibility is Platform Admin-only and is never exposed through ordinary shared read endpoints.

