# Map Preview

The moderation detail view shows a controlled coordinate preview:

- private source coordinates
- sanitized shared coordinates

This phase does not introduce new Google Maps content storage, map styles, or map IDs.

