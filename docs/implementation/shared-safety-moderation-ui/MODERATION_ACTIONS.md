# Moderation Actions

Implemented actions:

- approve and publish
- reject with required reason
- request correction with required note
- mark duplicate with required target record
- merge/link evidence to existing shared record
- retire shared record with required reason
- supersede shared record with required replacement and reason

Actions use explicit POST or PUT endpoints and require a valid admin action token.

