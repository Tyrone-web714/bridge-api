# Initial Evaluation Runs

Initial runs cover the three current datasets with offline/mock candidates. Each run includes eligible mock execution plus invalid, timeout or error, and unauthorized candidate observations. Structured datasets include malformed or schema-failing paths where applicable.

The generated artifacts do not represent production performance.

## Guardrails

- Repository fixtures only; no production data.
- Offline/mock execution only; no live hosted provider call.
- No weighted scores, thresholds, winners, provider rankings, cost-effectiveness claims, production recommendations, or runtime routing changes.
- No database persistence, migrations, deployments, or public API surface.

