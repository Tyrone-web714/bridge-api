# SAML Foundation

Generic SAML status: FOUNDATION ONLY.

Implemented:

- Tenant SAML provider configuration fields
- Entity ID, SSO URL, certificate reference, and metadata reference fields
- SAML ACS route contract
- Provider verification status reporting

Runtime XML signature validation is not implemented. A maintained SAML library and provider fixtures are required before SAML runtime verification.

