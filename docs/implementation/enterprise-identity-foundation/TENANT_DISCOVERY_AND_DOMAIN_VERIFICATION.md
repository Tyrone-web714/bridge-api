# Tenant Discovery and Domain Verification

Discovery supports verified domain lookup and Organization login identifiers.

Domains start in `PENDING` state with an explicit challenge. A domain is not authoritative until verified.

Ambiguous discovery fails safely. Client-supplied Organization or provider IDs are not treated as authorization proof.

External DNS verification is not claimed by this foundation.

