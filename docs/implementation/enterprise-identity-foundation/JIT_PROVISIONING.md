# JIT Provisioning

JIT status: POLICY FOUNDATION ONLY.

Default state is `DISABLED`.

JIT enablement requires Platform Admin approval in this foundation. JIT can never grant Platform Admin.

Actual customer-specific JIT behavior remains an operational and provider verification item.

