# Mobile SSO Integration

Mobile SSO status: FOUNDATION DOCUMENTED.

The backend now supports OIDC transaction state, nonce, PKCE challenge foundation, and tenant-bound session handoff concepts.

The React Native application was not changed in this phase. A future APK will be required when mobile SSO UI/deep-link handling is implemented.

Existing driver login remains unchanged.

