# Identity Audit Events

Identity events are recorded in `identity_security_events`.

Covered events include provider creation/update/disable, secret-reference rotation, domain verification, SSO initiation/success/failure, account linking, SCIM provisioning/deactivation, SSO policy updates, and break-glass requests.

Tokens, assertions, passwords, authorization codes, private keys, and raw secrets must not be logged.

