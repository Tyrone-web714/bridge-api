# Claim and Role Mapping

Claim mappings are Organization-scoped and provider-scoped.

Implemented safeguards:

- Only approved TSR roles are allowed.
- Platform Admin cannot be assigned through tenant mapping.
- Platform-scoped permissions cannot be granted through tenant mapping.
- Mappings are explicit and auditable.
- Disabled mappings do not apply.

