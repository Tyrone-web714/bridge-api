# Customer Context Contract

Customer context contains `customerContextId`, `organizationId`, stable customer/account ID, account reference, route/stop/delivery/invoice/product/deduction references, history window, source references, evidence timestamps, source hashes, completeness, confidence, unknown fields, human-review flag, and `testOnly`.
