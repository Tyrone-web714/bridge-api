# Knowledge Graph Integration

The existing knowledge graph can index Customer Intelligence docs, scripts, generated artifacts, and module files after regeneration. No graph runtime or production service is added.
