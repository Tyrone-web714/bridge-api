# Customer Identity Model

Stable account/customer identifiers are required. Customer display names must not become relational identifiers when account numbers or customer IDs exist. Similarly named customers across Organizations must not be linked.
