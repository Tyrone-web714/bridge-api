# Media Handling

Private hazard photos remain Organization-private.

Shared Safety publication may reference media only when:

- media has been reviewed
- private identifiers are removed
- metadata is sanitized
- Platform Admin explicitly approves the sanitized representation

Original private media URLs are not returned through shared read endpoints.

