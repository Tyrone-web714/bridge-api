# Platform Admin Exceptions

No new Platform Admin cross-Organization operation was introduced.

Current compatibility exception:

- Legacy bootstrap admin behavior remains for development and migration compatibility.

Required future work:

- Add explicit Platform Admin support workflow.
- Require explicit target Organization for cross-Organization support actions.
- Audit actor, target Organization, action, outcome, and request correlation ID.
