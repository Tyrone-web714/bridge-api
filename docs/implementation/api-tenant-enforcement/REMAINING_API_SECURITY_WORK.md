# Remaining API Security Work

Remaining work:

- Full HTTP integration matrix for every endpoint.
- API versioning under `/api/v1`.
- Explicit Platform Admin support workflow.
- Organization-scoped object-storage metadata enforcement.
- More granular permissions for AI/status, report exports, and route replay actions.
- Removal of legacy shared admin bootstrap and legacy driver API token after pilot compatibility is proven.
- Review lower-risk public reference endpoints and decide whether to keep them public as platform-global reference data.
