# Observability

Every selected-D2 response includes structured observability fields:

- capability ID;
- selected provider;
- selected model;
- success state;
- hard-gate pass state;
- fallback state;
- provider failure code where applicable;
- production routing flag.

Raw secrets are not logged or persisted by this package.
