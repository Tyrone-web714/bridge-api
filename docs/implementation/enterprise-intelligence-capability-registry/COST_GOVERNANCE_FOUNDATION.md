# Cost Governance Foundation

Cost metadata supports later TCO, ROI, and cost-per-success work without calculating those values now.

Monetary values use integer micro-USD. Unknown cost remains `null` or `UNKNOWN`; it is never silently treated as zero.

Provider, infrastructure, maintenance, retry, validation-failure, storage, monitoring, support, human-review, and failure-impact cost relevance can be recorded per capability.
