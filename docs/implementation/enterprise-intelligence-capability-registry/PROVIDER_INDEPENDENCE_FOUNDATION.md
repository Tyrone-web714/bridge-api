# Provider Independence Foundation

The registry separates business capability metadata from providers and models. Provider-specific execution remains isolated in `providerAdapters.js`.

Provider independence fields include providerSpecific, portabilityClassification, providerIndependenceStatus, replacementComplexity, and lockInAssessmentStatus.

The final provider lock-in scoring methodology remains deferred for owner approval.
