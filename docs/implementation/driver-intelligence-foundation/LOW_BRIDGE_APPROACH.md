# Low Bridge Approach

Low bridge approach detection compares current driver position to supplied upcoming bridge evidence using the Route Intelligence approach threshold. A nearby bridge produces a warning and human-review flag.

Missing position evidence is insufficient evidence. Missing bridge evidence is reviewable because the foundation cannot prove a clear path from absent data.

This is an advisory repository rule, not a production route-safety certification.
