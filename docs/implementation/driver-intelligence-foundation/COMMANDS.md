# Commands

Primary commands:

```powershell
npm.cmd run driver-intelligence:generate
npm.cmd run test:driver-intelligence
npm.cmd run driver-intelligence:validate
npm.cmd run driver-intelligence:check
npm.cmd run driver-intelligence:benchmarks
```

Integration commands:

```powershell
npm.cmd run knowledge-graph:generate
npm.cmd run dashboard-data:generate
npm.cmd run framework-validation:generate
npm.cmd test
```
