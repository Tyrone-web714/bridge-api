# Confidence Model

Confidence is evidence-bound.

Statuses:

- `KNOWN_EVIDENCE_ONLY` when required evidence is present.
- `EVIDENCE_GAPS_PRESENT` when one or more evidence categories are missing.

Confidence records are deterministic metadata and do not express model probability or predictive driver risk.
