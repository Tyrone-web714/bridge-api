# Knowledge Graph Integration

After regeneration, repository docs, scripts, generated artifacts, and module files are indexed by the existing knowledge graph. No production graph service is deployed.
