# Scope

Included: repository contracts, deterministic safety gates, synthetic fixtures, benchmark cases, generated artifacts, and validation scripts. Excluded: production route endpoint changes, provider calls, live optimization, deployments, migrations, object mutations, and production certification claims.
