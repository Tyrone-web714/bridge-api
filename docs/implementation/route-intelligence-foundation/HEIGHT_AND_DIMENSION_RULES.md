# Height And Dimension Rules

Height, width, and length measurements are normalized deterministically. When a route restriction has a maximum dimension, vehicle measurements above the maximum fail closed and unknown measurements require review.
