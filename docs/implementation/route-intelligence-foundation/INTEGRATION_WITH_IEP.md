# Integration With IEP

The foundation references the existing enterprise route.risk_explanation registry entry, route.intelligence.optimization orchestration capability, and lifecycle framework record. It does not enable runtime execution or hosted inference.
