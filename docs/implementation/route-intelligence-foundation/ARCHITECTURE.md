# Architecture

The foundation lives in bridge-api/services/intelligenceExecution/routeIntelligence.js and is executed only by repository scripts. Production routing remains in the existing routing API. Route intelligence consumes synthetic request objects and returns deterministic assessment evidence.
