# Tenant Context Boundary

Tenant context must be server-derived. Synthetic fixtures include organizationId only as repository test metadata. Caller supplied provider/model controls are not accepted as tenant or execution authority.
