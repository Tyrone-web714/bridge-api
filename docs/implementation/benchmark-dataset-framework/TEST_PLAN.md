# Test Plan

Run `npm run test:benchmark-datasets`, `npm run benchmark-datasets:check`, `npm run test:capability-registry`, `npm run test:intelligence-execution`, `npm run test:ai`, `npm run test:ai-architecture`, `npm run test:supervisor-intelligence`, `npm run test:api-tenant`, `npm run test:security`, and `npm test`.

Tests must not call live providers, access production databases, run migrations, deploy, or require production credentials.
