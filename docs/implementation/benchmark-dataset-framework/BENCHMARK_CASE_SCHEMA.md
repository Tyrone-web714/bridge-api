# Benchmark Case Schema

Benchmark cases are embedded in each authoritative `dataset.json`. Cases include identity, input payload and metadata, expected-result fields, assertions, reference evaluation data, risk metadata, provenance, and control fields.

Cases support deterministic and probabilistic evaluation without requiring fragile free-text golden answers where structured assertions are more appropriate.
