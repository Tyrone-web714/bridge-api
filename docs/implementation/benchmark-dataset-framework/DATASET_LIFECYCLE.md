# Dataset Lifecycle

Dataset lifecycle states are `DRAFT`, `VALIDATING`, `APPROVED_FOR_TEST`, `BENCHMARK_READY`, `FROZEN`, `DEPRECATED`, and `RETIRED`. Lifecycle is distinct from enabled status, approval status, privacy classification, freshness, quality, capability lifecycle, benchmark execution status, and storage availability.

Initial datasets are `APPROVED_FOR_TEST`, not benchmark-ready or frozen.
