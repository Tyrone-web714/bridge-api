# Dataset Template Guide

Use `bridge-api/benchmarks/templates/dataset-template.json` as the conservative starting point for new datasets. Defaults are draft, disabled, pending approval, local-test only, no production data, no approved real-data use, and explicit unknowns.

Do not insert owner approvals, production benchmark access, or real-data claims without a future approved decision record.
