# Synthetic Data Standard

Synthetic data must not reproduce real customer, employee, driver, route, invoice, product, Organization, credential, or production object-storage data. Synthetic fixtures use deterministic seeds and generation metadata.

This work includes only manual synthetic fixtures and the metadata contract, not a large synthetic-data engine.
