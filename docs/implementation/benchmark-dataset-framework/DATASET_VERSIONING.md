# Dataset Versioning

Dataset release versions use semantic versioning. Schema, fixture format, input contract, expected-result contract, evaluation-reference, sanitization, provenance, capability contract, and reserved benchmark protocol versions remain distinct.

Published frozen versions are immutable. Corrections require a new version rather than rewriting historical evidence.
